# Root Artifacts Archive

This folder stores root-level imported/legacy artifacts moved out of the project root to keep the repository organized.

Moved here intentionally:
- imported duplicates (`*__imported*`)
- transient logs (`error*.log`, `pytest_full.log`, audit logs)
- imported env/config copies not used by active runtime
