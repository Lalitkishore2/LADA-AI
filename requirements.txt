# LADA - Language Agnostic Digital Assistant
# Python 3.14+ Compatible (verified 2026-04)
#
# Install: pip install -r requirements.txt
# Post-install: python -m spacy download en_core_web_sm
# Note: pyaudio and pygame are NOT installable on Python 3.14 without
#       Microsoft C++ Build Tools. Use sounddevice+soundfile instead.

# ============ CORE ============
python-dotenv>=1.0.0
requests>=2.31.0
aiohttp>=3.9.0
psutil>=5.9.6

# ============ GUI ============
PyQt5>=5.15.10

# ============ AI BACKENDS ============
google-genai>=1.0.0              # Google Gemini API (new SDK)
# ollama>=0.1.18                  # Optional: Ollama Python client

# ============ VOICE ============
pyttsx3>=2.90                    # Offline text-to-speech (sapi5 on Windows)
SpeechRecognition>=3.10.0        # Speech-to-text (wraps Google/Whisper)
gTTS>=2.4.0                      # Google text-to-speech (online, natural)
# pygame>=2.5.2                  # Audio playback — NOT buildable on Python 3.14+
#                                  Use sounddevice+soundfile instead (see below)
sounddevice>=0.4.0               # Cross-version audio I/O (replaces pyaudio + pygame)
soundfile>=0.12.0                # Audio file read/write (WAV, MP3 via libsndfile)
openwakeword>=0.6.0              # Local offline wake word detection
# pyaudio>=0.2.13               # NOT buildable on Python 3.14 without MSVC
#                                  Use sounddevice instead

# ============ SYSTEM CONTROL ============
pycaw>=20181226                  # Windows audio control
screen-brightness-control>=0.0.1 # Display brightness
pyperclip>=1.8.2                 # Clipboard operations
keyboard>=0.13.5                 # Global hotkeys
pyautogui>=0.9.54                # GUI automation, screenshots

# ============ BROWSER AUTOMATION ============
selenium>=4.10.0                 # WebDriver browser control
beautifulsoup4>=4.12.2           # HTML parsing

# ============ GOOGLE SERVICES ============
google-api-python-client>=2.108.0 # Gmail, Calendar APIs
google-auth-oauthlib>=1.1.0       # OAuth2 authentication
google-auth-httplib2>=0.1.1       # HTTP transport

# ============ NLU & TEXT PROCESSING ============
spacy>=3.7.2                     # NLU engine, entity extraction
fuzzywuzzy>=0.18.0               # Fuzzy string matching
python-Levenshtein>=0.20.0       # Fast string distance

# ============ SCHEDULING ============
schedule>=1.2.0                  # Simple periodic scheduling
APScheduler>=3.10.0              # Advanced cron-style scheduling
python-dateutil>=2.8.2           # Date parsing

# ============ FILE OPERATIONS ============
send2trash>=1.8.1                # Safe file deletion

# ============ DOCUMENT PROCESSING ============
pdfplumber>=0.10.3               # PDF text extraction
python-docx>=1.1.0               # Word document read/write
reportlab>=4.0.8                 # PDF generation
PyMuPDF>=1.23.8                  # PDF rendering (fitz)

# ============ SECURITY ============
cryptography>=41.0.7             # File encryption (Fernet/AES)

# ============ NLP & ANALYSIS ============
textblob>=0.17.1                 # Sentiment analysis
Pillow>=10.0.0                   # Image processing

# ============ UTILITIES ============
tqdm>=4.65.0                     # Progress bars
humanize>=4.9.0                  # Human-readable formatting
colorama>=0.4.6                  # Terminal colors
Pygments>=2.17.2                 # Syntax highlighting
markdown>=3.5.1                  # Markdown rendering
lxml>=4.9.0                      # XML/HTML parsing

# ============ WEB API SERVER ============
fastapi>=0.104.0                 # REST API server
uvicorn[standard]>=0.24.0        # ASGI server (includes websockets support)

# ============ CODE SANDBOX ============
RestrictedPython>=7.0            # Safe code execution

# ============ DATA ============
pandas>=2.0.3                    # Data analysis, CSV export

# ============ OPTIONAL: VECTOR MEMORY ============
# chromadb>=0.4.22               # Semantic search (requires large install)
# sentence-transformers>=2.2.2   # Embedding models (requires PyTorch)

# ============ OPTIONAL: ADVANCED VOICE ============
faster-whisper>=1.0.0            # Fast offline STT (base.en model default)
edge-tts>=6.1.0                  # Microsoft Edge Neural TTS (en-US-AriaNeural)
# openai-whisper>=20231117       # OpenAI Whisper STT (alternative to faster-whisper)
# pydub>=0.25.1                  # Audio manipulation (not needed if using sounddevice)
# pyaudio>=0.2.13                # SKIP on Python 3.14+ — not buildable without MSVC

# ============ OPTIONAL: XTTS-v2 TTS ============
# TTS>=0.21.0                    # Coqui XTTS-v2 (voice cloning, 17 languages)
# torch>=2.0.0                   # PyTorch (required for XTTS-v2)
# torchaudio>=2.0.0              # Audio processing

# ============ OPTIONAL: VISION & OCR ============
opencv-python>=4.8.0           # Computer vision
pygetwindow>=0.0.9             # Window management
# pytesseract>=0.3.10            # OCR text extraction

# ============ OPTIONAL: AI PROVIDERS ============
# openai>=1.0.0                  # OpenAI API
# anthropic>=0.18.0              # Anthropic Claude API

# ============ OPTIONAL: MESSAGING CONNECTORS ============
# python-telegram-bot>=20.0      # Telegram bot
# discord.py>=2.3.0              # Discord bot
# slack-sdk>=3.23.0              # Slack bot
# pywhatkit>=5.4                 # WhatsApp automation

# ============ OPTIONAL: REAL-TIME VOICE ============
# livekit>=0.11.0                # LiveKit real-time communication
# deepgram-sdk>=3.0.0            # Deepgram STT API

# ============ OPTIONAL: IMAGE GENERATION ============
# stability-sdk>=0.8.0           # Stable Diffusion API

# ============ OPTIONAL: YOUTUBE ============
# youtube-transcript-api>=0.6.0  # YouTube transcript extraction

# ============ OPTIONAL: BROWSER (PLAYWRIGHT) ============
playwright>=1.40.0              # Chromium automation (v12.0 primary driver)

# ============ STEALTH BROWSER ============
undetected-chromedriver>=3.5.0   # Stealth Chrome automation
websocket-client>=1.6.0          # CDP WebSocket connection

# ============ ALEXA INTEGRATION ============
# flask-ask-sdk>=1.0.0           # Alexa Skills Kit
# ngrok>=0.8.0                   # HTTPS tunnel for Alexa

# ============ SYSTEM TRAY ============
pystray>=0.19.0                  # System tray icon

# ============ MOLTBOT ============
pyserial>=3.5                    # Serial communication

# ============ LADA v12.0: NEW SUBSYSTEMS ============
edge-tts>=6.1.0                  # Microsoft Edge TTS (Phase 3 voice pipeline)
# mem0ai>=0.1.0                  # Mem0 semantic memory (Phase 5, optional)
# openwakeword>=0.6.0            # Wake-word detection (Phase 3, listed above)
# faster-whisper>=1.0.0          # Fast STT (Phase 3, listed above)

# ============ TESTING ============
# pytest>=7.4.0
# pytest-asyncio>=0.21.0

# ============ POST-INSTALL STEPS ============
# python -m spacy download en_core_web_sm
# python -m textblob.download_corpora
# playwright install    (if using playwright)
