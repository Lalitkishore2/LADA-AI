import { resolveActiveTalkProviderConfig } from "../../config/talk.js";
import type { LADAConfig } from "../../config/types.js";

export { resolveActiveTalkProviderConfig };

export function getRuntimeConfigSnapshot(): LADAConfig | null {
  return null;
}

