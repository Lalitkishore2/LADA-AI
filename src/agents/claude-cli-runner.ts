// Backwards-compatible entry point.
// Implementation lives in `src/agents/cli-runner.ts` (so we can reuse the same runner for other CLIs).
export { runLADACliAgent, runCliAgent } from "./cli-runner.js";

